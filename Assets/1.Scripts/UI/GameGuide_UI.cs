using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GameGuide_UI : MonoBehaviour
{
    [SerializeField] private ToggleGroup tgroup;
    [SerializeField] private List<Toggle> tg_list = new List<Toggle>();
    [SerializeField] private List<GameObject> pg_list = new List<GameObject>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
